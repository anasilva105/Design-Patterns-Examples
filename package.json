{
  "name": "design-patterns-examples",
  "version": "1.0.0",
  "description": "This project contains some examples of design patterns",
  "main": "main.ts",
  "scripts": {
    "build": "npx tsc --build",
    "clean": "npx tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && npm run build && node dist/main.js"
  },
  "keywords": [
    "design",
    "patterns"
  ],
  "author": "Ana Silva",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.16.3",
    "typescript": "^5.0.4"
  }
}
